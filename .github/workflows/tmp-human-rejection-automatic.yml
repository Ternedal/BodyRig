name: tmp-human-rejection-automatic

on:
  push:
    branches: [hardening/human-fidelity-rejection]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: hardening/human-fidelity-rejection
      - run: python tools/tmp_patch_rejection_automatic.py
      - run: |
          rm tools/tmp_patch_rejection_automatic.py
          rm .github/workflows/tmp-human-rejection-automatic.yml
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Acceptance: block automatic activation after human rejection"
          git push origin HEAD:hardening/human-fidelity-rejection
