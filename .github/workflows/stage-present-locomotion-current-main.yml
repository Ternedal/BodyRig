name: stage-present-locomotion-current-main

on:
  push:
    branches:
      - staging/present-locomotion-current-main-20260911

permissions:
  contents: write

jobs:
  stage:
    if: ${{ !contains(github.event.head_commit.message, '[present-current-main-applied]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - name: Install locked Linux test environment
        run: python -m pip install --disable-pip-version-check -c requirements/linux-ci-python.lock.txt -e ".[test]"
      - name: Apply patch
        run: python .github/apply_present_locomotion_current_main.py
      - name: Run combined renderer regressions
        run: python -m pytest -q tests/test_reference_renderer_gesture_arm_ownership_contract.py tests/test_reference_renderer_unsupported_gesture_locomotion_arms_contract.py tests/test_reference_renderer_gesture_release_ownership.py tests/test_reference_renderer_motor_v3_contract.py tests/test_reference_renderer_motor_v2_contract.py
      - name: Check whitespace
        run: git diff --check
      - name: Commit staged files
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reference-renderer/Assets/BodyRig/BodyRigMotorDriver.cs tests/test_reference_renderer_gesture_arm_ownership_contract.py tests/test_reference_renderer_unsupported_gesture_locomotion_arms_contract.py
          git commit -m "Preserve locomotion on incomplete present rigs [present-current-main-applied]"
          git push origin HEAD:${{ github.ref_name }}
